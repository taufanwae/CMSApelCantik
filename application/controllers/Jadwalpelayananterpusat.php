<?php
/* 
 * Generated by CRUDigniter v3.2 
 * www.crudigniter.com
 */
 
class Jadwalpelayananterpusat extends CI_Controller{
    function __construct()
    {
        parent::__construct(); 
        if (!$this->session->userdata('login')) {
            redirect('login','index');
        }
        $this->load->model('M_jadwalpelayananterpusat');
    } 

    /*
     * Listing of info_jadwalpelayananterpusat
     */
    function index()
    {
        $params['limit'] = RECORDS_PER_PAGE; 
        $params['offset'] = ($this->input->get('per_page')) ? $this->input->get('per_page') : 0;
        
        $config = $this->config->item('pagination');
        $config['base_url'] = site_url('jadwalpelayananterpusat/index?');
        $config['total_rows'] = $this->M_jadwalpelayananterpusat->get_all_info_jadwalpelayananterpusat_count();
        $this->pagination->initialize($config);

        $data['info_jadwalpelayananterpusat'] = $this->M_jadwalpelayananterpusat->get_all_info_jadwalpelayananterpusat($params);
        
        $data['_view'] = 'jadwalpelayananterpusat/index';
        $this->load->view('layouts/main',$data);
    }

    /*
     * Adding a new info_jadwal_pelayanan_tetap
     */
    function add()
    {   
        $this->load->library('form_validation');

		$this->form_validation->set_rules('tanggal','Tanggal','required');
		$this->form_validation->set_rules('jam_mulai','Jam Mulai','required|max_length[5]');
		$this->form_validation->set_rules('jam_selesai','Jam Selesai','required|max_length[5]');
        $this->form_validation->set_rules('keterangan','Keterangan / Info','max_length[250]');
        $this->form_validation->set_rules('tempat_pelayanan','Tempat pelayanan','max_length[250]');
		
		if($this->form_validation->run())     
        {   
            $params = array(
				'kecamatan_id' => $this->input->post('kecamatan_id'),
                'tempat_pelayanan' => $this->input->post('tempat_pelayanan'),
				'tanggal' => $this->input->post('tanggal'),
				'jam_mulai' => $this->input->post('jam_mulai'),
				'jam_selesai' => $this->input->post('jam_selesai'),
                'created_by'    => $this->session->userdata('admin_id'),
                'keterangan'    => $this->input->post('keterangan'),
                'created_at'    => date('Y-m-d H:i:s')
            );
            
            $info_jadwal_pelayanan_tetap_id = $this->M_jadwalpelayananterpusat->add_info_jadwal_pelayanan_tetap($params);
            redirect('jadwalpelayananterpusat/index');
        }
        else
        {
			$this->load->model('M_listkecamatan');
			$data['all_mstlokasipelayanan'] = $this->M_listkecamatan->get_all_mstkecamatan();
            
            $data['_view'] = 'jadwalpelayananterpusat/add';
            $this->load->view('layouts/main',$data);
        }
    }  

    /*
     * Editing a info_jadwal_pelayanan_tetap
     */
    function edit($info_jadwal_id)
    {   
        // check if the info_jadwal_pelayanan_tetap exists before trying to edit it
        $data['info_jadwal_pelayanan_terpusat'] = $this->M_jadwalpelayananterpusat->get_info_jadwal_pelayanan_tetap($info_jadwal_id);
        
        if(isset($data['info_jadwal_pelayanan_terpusat']['jadwal_id']))
        {
            $this->load->library('form_validation');

			// $this->form_validation->set_rules('lokasipelayanan_id','Lokasipelayanan Id','required');
			$this->form_validation->set_rules('tanggal','Tanggal','required');
			$this->form_validation->set_rules('jam_mulai','Jam Mulai','required');
			$this->form_validation->set_rules('jam_selesai','Jam Selesai','required');
            $this->form_validation->set_rules('keterangan','Keterangan / Info','max_length[250]');
            $this->form_validation->set_rules('tempat_pelayanan','Tempat pelayanan','max_length[250]');
		
			if($this->form_validation->run())     
            {   
                $params = array(
                    'kecamatan_id' => $this->input->post('kecamatan_id'),
                    'tempat_pelayanan' => $this->input->post('tempat_pelayanan'),
					'tanggal' => $this->input->post('tanggal'),
					'jam_mulai' => $this->input->post('jam_mulai'),
					'jam_selesai' => $this->input->post('jam_selesai'),
                    'created_by'    => $this->session->userdata('admin_id'),
                    'keterangan'    => $this->input->post('keterangan'),
                );

                $this->M_jadwalpelayananterpusat->update_info_jadwal_pelayanan_tetap($info_jadwal_id,$params);            
                redirect('jadwalpelayananterpusat/index');
            }
            else
            {
				$this->load->model('M_listkecamatan');
                $data['all_mstlokasipelayanan'] = $this->M_listkecamatan->get_all_mstkecamatan();

                $data['_view'] = 'jadwalpelayananterpusat/edit';
                $this->load->view('layouts/main',$data);
            }
        }
        else
            show_error('The info_jadwal_pelayanan_terpusat you are trying to edit does not exist.');
    } 

    /*
     * Deleting info_jadwal_pelayanan_tetap
     */
    function remove($info_jadwal_id)
    {
        $info_jadwal_pelayanan_tetap = $this->M_jadwalpelayananterpusat->get_info_jadwal_pelayanan_tetap($info_jadwal_id);

        // check if the info_jadwal_pelayanan_tetap exists before trying to delete it
        if(isset($info_jadwal_pelayanan_tetap['jadwal_id']))
        {
            $this->M_jadwalpelayananterpusat->delete_info_jadwal_pelayanan_tetap($info_jadwal_id);
            redirect('jadwalpelayananterpusat/index');
        }
        else
            show_error('The info_jadwal_pelayanan_tetap you are trying to delete does not exist.');
    }
    
}
